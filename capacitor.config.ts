import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.extralifeinstruments.er99',
  appName: 'ER-99',
  webDir: 'www',
  android: {
    backgroundColor: '#111111'
  }
};

export default config;
